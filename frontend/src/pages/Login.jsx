import { LockKeyhole } from 'lucide-react';
import { useState } from 'react';
import { useAuth } from '../context/AuthContext';
export function Login() { const { signIn } = useAuth(); const [username,setUsername]=useState('admin'); const [password,setPassword]=useState('admin123'); const [error,setError]=useState(''); async function submit(e){e.preventDefault(); try{await signIn(username,password)}catch{setError('Credenciales inválidas')}} return <div className="min-h-screen grid place-items-center p-6"><form onSubmit={submit} className="glass w-full max-w-md rounded-[2rem] p-8"><div className="h-16 w-16 rounded-3xl bg-gradient-to-br from-cyan to-gold grid place-items-center text-night mb-6"><LockKeyhole size={30}/></div><h1 className="text-3xl font-black">Panel de Sobres</h1><p className="text-slate-400 mt-2">Acceso administrativo seguro</p><div className="space-y-3 mt-8"><input className="premium-input w-full" value={username} onChange={e=>setUsername(e.target.value)} placeholder="Usuario"/><input className="premium-input w-full" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Contraseña" type="password"/></div>{error&&<p className="text-rose-300 mt-3">{error}</p>}<button className="mt-6 w-full rounded-2xl bg-gradient-to-r from-cyan to-gold text-night font-black py-3">Entrar</button></form></div> }
