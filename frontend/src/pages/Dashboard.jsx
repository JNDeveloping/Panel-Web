import { motion } from 'framer-motion';
import { Banknote, Package, ShieldAlert, Vault } from 'lucide-react';
import { CashMissingChart, RankingChart } from '../components/Charts';
import { MovementForm } from '../components/MovementForm';
import { MovementsTable } from '../components/MovementsTable';
import { StatCard } from '../components/StatCard';
export function Dashboard({ data, createMovement }) { const s=data?.summary||{}; return <motion.div initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} className="space-y-6"><div className="flex flex-col md:flex-row md:items-center justify-between gap-4"><div><p className="text-cyan font-semibold">Control en tiempo real</p><h1 className="text-4xl font-black">Dashboard financiero</h1></div><div className="glass rounded-2xl px-5 py-3 text-sm text-slate-300">dinero total = efectivo + faltante</div></div><div className="grid md:grid-cols-2 xl:grid-cols-4 gap-5"><StatCard title="Sobres físicos" value={s.envelopes||0} icon={Package} accent="from-cyan to-blue-500" subtitle="ENTRA + VACIO"/><StatCard title="Dinero total" value={`$${Number(s.total||0).toLocaleString()}`} icon={Vault} accent="from-gold to-amber-500" subtitle="Efectivo + faltante"/><StatCard title="Efectivo en caja" value={`$${Number(s.cash||0).toLocaleString()}`} icon={Banknote} accent="from-emerald-300 to-cyan" subtitle="Caja disponible"/><StatCard title="Faltante" value={`$${Number(s.missing||0).toLocaleString()}`} icon={ShieldAlert} accent="from-rose-400 to-orange-400" subtitle="Pendiente de ingreso"/></div><MovementForm onCreate={createMovement}/><div className="grid xl:grid-cols-2 gap-6"><CashMissingChart data={data?.daily||[]}/><RankingChart data={data?.byUser||[]}/></div><div><h2 className="text-2xl font-bold mb-4">Últimos movimientos</h2><MovementsTable rows={data?.latest||[]}/></div></motion.div> }
